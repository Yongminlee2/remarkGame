.gradle/
build/
app/build/
local.properties
.idea/
.kotlin/
*.apk
keystore.properties
*.jks
*.keystore

# 뜻풀이 생성기의 중간 파일(10MB). db.sql 에서 언제든 다시 만든다 —
# tools/extract_kkutu_meta.py 주석 참고
tools/kkutu_meta.tsv

# 파이썬이 스크립트를 실행하면서 남기는 캐시. 실수로 커밋된 적이 있다.
__pycache__/
*.pyc

# 릴리스 산출물(20MB AAB). 빌드하면 다시 나온다.
release/
*.aab

# Firebase 설정. 비밀번호는 아니지만 공개 저장소에 두면 자동 탐지 경고가 뜨고,
# 실제 보호는 보안 규칙이 한다. 우리아이 지킴이도 같은 방식으로 뺐다.
app/google-services.json
